const rootDir = __dirname;
module.exports = {
    port: 8000,
    rootDir,
    db: {
        host: "localhost",
        user: "root",
        password: "root@root",
        database: "resources"
    }
};